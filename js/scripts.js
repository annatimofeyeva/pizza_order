//business logic
function Pizza (sizes, toppings, meats, cost){
  this.sizes = size;
  this.toppings = toppings;
  this.meats = meats;
  this.cost = cost;
}

Pizza.prototype.calculateCost function(){
 if (inputtedSizes === "large ("14")")) {
   this. cost += 2;
 } else if (inputtedSizes === "medium ("12")")) {
   this.cost += 3;
 } else (inputtedSizes === "small ("10")") {
   this.cost += 4;)
}
}
